
class DTOInserterP2():
        def __init__(self, idog, isActive, code, zone, zip_code, province_name, locality_name, enabled_place):
            self.idog = idog
            self.isActive = isActive
            self.code = code
            self.zone = zone
            self.zip_code = zip_code
            self.province_name = province_name
            self.locality_name = locality_name
            self.enabled_place = enabled_place
